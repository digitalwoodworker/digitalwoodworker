image: node:18

stages:
  - build

cache:
  paths:
    - node_modules/

pages:
  stage: build
  script:
    - npm ci
    - npm run build
    - rm -rf public || true
    - cp -r dist public
  artifacts:
    paths:
      - public
  only:
    - master
